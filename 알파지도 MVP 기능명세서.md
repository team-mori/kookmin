# 알파지도(가칭) — MVP 분할 기능명세서

**팀** 입체기동대 · **근거 문서** 알파프로젝트신청서.pdf (서비스 기능 부분만 발췌, 활동 계획·예산은 범위 외)

한 줄 정의: **캠퍼스 생활 정보가 건물·층에 1:1로 귀속되어 쌓이는, 2.5D 일러스트 지도 기반 커뮤니티.**
기존 커뮤니티가 "글에서 출발해 장소를 찾아가는" 구조라면, 이 서비스는 "장소에서 출발해 그곳에 쌓인 정보에 도달하는" 구조다.

---

## 0. 공통 원칙 (계획서에서 도출)

1. **모든 게시물은 건물·층 식별자에 귀속된다.** 시간순 피드는 없다. 한 번 올라온 정보는 소모되지 않고 해당 공간에 남아 다음 이용자에게 다시 쓰인다.
2. **위치정보는 기기 안에서만 쓴다.** GPS는 "지금 어느 건물인가" 판별 용도로만 사용하고, 서버에 이동 이력을 저장하지 않는다.
3. **각 MVP는 독립적으로 배포 가능한 완성품이어야 한다.** 다음 단계가 없어도 그 자체로 쓸 수 있어야 한다.
4. 최종 완성 기준(계획서 명시): **첫 화면 핫이슈 스토리 → 건물 탐색 → 층별 게시글 확인 → 길찾기**로 이어지는 사용 흐름이 끊김 없이 동작하는 상태.

---

## MVP 1 — 지도 위 게시판 (코어 루프)

> 목적: "장소에서 출발해 정보에 도달한다"는 핵심 구조를 최소 형태로 증명. 클로즈드 베타(지인) 배포 가능 수준.

### 1.1 2.5D 일러스트 캠퍼스 지도 뷰어
- 팀 제작 2.5D 일러스트 지도를 기본 화면으로 표시한다
- 핀치 줌 · 팬 제스처, 이동 제한 범위(캠퍼스 밖으로 나가지 않음)
- 건물을 탭하면 해당 건물의 하단 시트가 열린다
- 일러스트의 픽셀 좌표는 실제 위경도와 보정(기준점 대응)되어 있어야 한다 — MVP 2의 현위치 표시가 이 보정 위에서 동작한다

### 1.2 건물 하단 시트
- 구성: 건물명 · 건물 일러스트 · 층 선택 탭(예: B1 / 1F / … / 11F) · 층별 게시글 목록
- 층 탭에 게시글 개수 배지를 표시한다
- 게시글을 탭하면 상세(모달)가 열린다

### 1.3 게시글 작성·조회
- 작성 시 건물·층을 **수동 선택**한다 (GPS 자동 제안은 MVP 2)
- 필드: 카테고리(정보 / 수다 / 분실물 / 기타) · 제목 · 내용 · 사진 첨부 · 상세 위치 텍스트(예: "우측 엘리베이터 앞") · 익명 여부
- 조회: 건물·층 기준 목록, 최신순

### 1.4 학교 이메일 인증
- @kookmin.ac.kr 이메일로 인증코드 발송·검증 → 가입
- 닉네임 관리. 글 작성은 인증된 계정만, 조회는 비로그인 허용

### MVP 1 완료 조건
- [ ] 지도에서 건물 탭 → 층 탭 → 그 층의 글 목록 → 글 상세까지 도달
- [ ] 인증된 계정으로 특정 건물·층에 글 작성, 다른 기기에서 같은 위치 조회 시 노출
- [ ] 기초 데이터: 캠퍼스 주요 건물 약 20개 동의 건물·층 구조 입력

### MVP 1에서 명시적으로 제외
GPS·현위치, 댓글·공감, TTL 만료, 검색, 길찾기, 핫이슈 스토리, 신고·운영 도구

---

## MVP 2 — 살아있는 커뮤니티 (교내 공개 배포선)

> 목적: 정보가 순환하는 커뮤니티로 만들고, 불특정 다수에게 공개해도 되는 운영 체계를 갖춘다. **공개 배포의 최소 조건.**

### 2.1 현위치와 건물 자동 인식
- 지도 위 현위치 핀 표시 (픽셀↔위경도 보정 활용)
- 글 작성 시 GPS로 현재 건물을 자동 제안, 오인식 대비 수동 선택 유지
- 판별은 기기 내 처리, 서버에 위치 이력 저장 금지

### 2.2 반응과 소통
- 게시글 공감: 「도움돼요」 「확인했어요」 (계획서 시안 문구 기준)
- 댓글 (대댓글 1단까지)

### 2.3 정보의 수명 관리 (TTL)
- 카테고리별 유효 기간: 실시간 제보·분실물처럼 유효 기간이 짧은 정보는 만료 시 목록에서 내려가고 아카이브로 이동
- 축적 가치가 있는 정보(시설 정보 등)는 만료 없이 남는다
- 만료 정보는 화면을 차지하지 않되, 검색·아카이브에서는 열람 가능

### 2.4 운영 체계 (공개 배포 전 필수)
- 신고 접수 → 24시간 내 검토 → 블라인드 처리 절차
- 블라인드된 글·댓글은 목록에서 숨김 처리
- 이용 지표 로깅 시작: 가입 · 게시 · 검색 행동 (개인 식별 불가 형태)

### 2.5 지도 정보 밀도 제어
- 건물 마커에 대표 게시글·개수 배지(예: +9) 표시
- 좁은 화면에서 마커가 겹치면 클러스터링 처리
- 다수 마커·오버레이 동시 표시 상황에서 지도 조작 60fps 유지

### MVP 2 완료 조건
- [ ] 현장에서 글 작성 시 현재 건물이 자동 제안됨 (몇 초 안에 등록 가능)
- [ ] 신고 → 블라인드 흐름이 실제로 동작
- [ ] 분실물 글이 유효 기간 후 자동 만료됨
- [ ] 교내 공개 배포 (에브리타임·단톡 홍보 가능 상태)

---

## MVP 3 — 입체 길찾기

> 목적: 고저차 캠퍼스 특화 길찾기. 네이버·카카오가 다루지 않는 교내 도보 경로와 수직 이동을 제공하는 차별화 기능.

### 3.1 경로 데이터
- 현장 답사로 구축한 노드·간선 그래프: 경사도, 계단, 엘리베이터, 건물 간 연결 통로 포함
- 갈림길 노드에 랜드마크 실물 사진 + 진행 방향 화살표 메타데이터
- 데이터 입력·관리 도구(시트 수준)와 그래프 정합성 검증(누락·중복·단절 노드 탐지)

### 3.2 경로 계산
- 가중 그래프 최단 경로(다익스트라/A*)
- 경로 옵션: **경사 회피** / 최단 시간 — 비용 함수 분리
- 실측 도보 소요시간과 산출값 대조로 가중치 보정

### 3.3 경로 안내 화면
- 2.5D 지도 위 구간별 경로 폴리라인, 수직 이동(엘리베이터·연결 통로) 구간 전용 스타일
- **구간별 타임라인**: 소요시간 · 거리 · 안내 문구
- 갈림길마다 랜드마크 사진 + 방향 화살표 팝업 — 지도를 잘 못 읽는 사용자도 사진 대조로 도달 가능하게
- 출발/도착 입력: 건물 선택 + 현위치(실외 GPS)

### MVP 3 완료 조건
- [ ] 임의 건물 A→B 경로가 경사 회피 옵션 켠 상태에서 오르막 회피 경로로 안내됨
- [ ] 갈림길에서 랜드마크 사진 안내가 표시됨
- [ ] 대표 경로 50쌍 기준 경로 탐색 회귀 테스트 통과

---

## MVP 4 — 매일 여는 앱

> 목적: 도구(필요할 때만 여는 앱)에서 습관(매일 여는 앱)으로. 계획서의 최종 완성 기준을 닫는 단계.

### 4.1 핫이슈 스토리
- 건물별로 공감·댓글이 모인 게시물을 "오늘 북악관 이슈" 형태로 묶어 첫 화면 상단에 스토리 형식으로 노출
- 탭 넘김 인터랙션, 자동 재생, 스토리에서 해당 건물·게시글로 바로 이동
- 사용자 반응 기반 실시간 집계

### 4.2 성과 지표 정리 (학술 병행)
- 인증 가입자 수, 건물별 누적 게시글 수, 길찾기 이용 건수·안내 완료율, 재방문율 집계
- 개인 식별 불가 형태, 학기 말 논문 실증 데이터로 활용

### MVP 4 완료 조건
- [ ] 앱 실행 → 스토리 → 건물 탐색 → 층별 게시글 → 길찾기 흐름이 끊김 없이 동작 (계획서 완성 기준)

---

## 부록 — 기존 문서·코드와의 관계

| 항목 | 기존 (`대국민지도 기능명세서.md` + 현재 코드) | 계획서 (이 문서의 기준) |
|---|---|---|
| 서비스 축 | 실내 길찾기 중심 (커뮤니티 없음) | 커뮤니티 중심 + 길찾기는 3단계 |
| 공간 단위 | 호실 단위 실내 지도 (공학관 층 도면 구현됨) | **건물·층 단위** (호실·실내 도면 없음) |
| 지도 | MapLibre 벡터 | 팀 제작 2.5D 일러스트 |
| AR | 3순위 로드맵 | 없음 |

현재 레포의 공학관 실내(호실 단위) 코드는 계획서 범위보다 세밀한 단위다. 계획서 기준으로 가면 실내 도면·호실 검색은 이번 학기 범위 밖이며, 기존 코드는 참고 자산으로 남긴다.
