# 대국민지도 — 기능명세서

**작성 주체** 모리(MORI) · **연동 문서** PRD, 데이터 모델 정의서

이 문서는 사용자와 학교 부서가 마주하는 기능의 동작을 정의합니다. 데이터 구조와 구축 절차는 별도의 데이터 모델 정의서를 따릅니다.

---

## 1. 서비스 원칙

- 안내는 **지도와 화살표 경로선**으로 완결한다. 문구 안내 카드, 구간별 사진, 단계 진행(스테퍼) 방식은 사용하지 않는다. 사용자는 지도에 그려진 화살표 길만 보고 목적지에 도달할 수 있어야 한다.
- 실내에서는 실시간 위치 추적을 사용하지 않는다. 대신 지도 위의 호실 표시가 이정표 역할을 한다.
- 각 단계(검색 → 길찾기 → AR)는 독립적으로 배포 가능한 완성품이어야 한다.

---

## 2. 세부 장소 검색·조회 (1순위)

### 2.1 검색

- 검색 대상: 건물(정식 명칭·별칭), 강의실 호실, 이름 있는 장소(강당·세미나실·학과 사무실 등), 실외 장소(정문·광장·운동장·정류장 등)
- **모든 장소는 개별 개체로 등록된다.** 강의실 하나하나("북악관 1517호")가 독립된 장소이며, 층은 그 장소에 딸린 보조 정보다. "북악관 1517"을 검색하면 등록된 1517호 개체가 직접 검색된다
- 결과는 관련도 순으로 정렬해 목록으로 제시한다

**예외 처리** — 입력한 호실이 등록되어 있지 않으면 "등록되지 않은 장소"임을 알리고, 해당 건물 카드와 유사 검색어를 제안한다.

### 2.2 장소 정보 카드

장소를 선택하면 카드가 열린다. 구성:

- 건물 외관·입구 사진 (강의실 내부 사진은 두지 않는다)
- 위치 요약 (건물, 층)
- **가장 유리한 입구와 진입 층 안내** — 어느 문으로 들어가면 몇 층인지
- 층별 주요 시설
- [지도에서 보기] — 해당 건물의 실내 층별 뷰가 곧장 열리고 이 장소가 강조된다
- [길찾기] 버튼 — 이 장소를 도착지로 길찾기 시작

### 2.3 공유 링크

모든 장소는 고유 주소를 가진다. 링크로 접근하면 해당 카드가 바로 열린다. 학내 공지·커뮤니티에 붙이는 용도를 전제한다.

---

## 3. 길찾기 (2순위)

### 3.1 출발지·도착지

실내 장소, 실외 장소, 현재 위치(실외에서 GPS)를 자유롭게 조합한다. 실내에서의 "현재 위치" 지정은 AR 단계 도입 전까지 장소 선택으로 대체한다.

### 3.2 경로 계산

- 캠퍼스 동선 그래프에서 소요 시간이 가장 짧은 경로를 계산한다 (브라우저에서 즉시 계산)
- 현재 시각에 닫혀 있는 문·통로는 경로에서 제외한다
- **목적 층 기준 최적 입구** — 도착 건물에 입구가 여럿이면, 목적 층까지 총비용(시간·계단)이 가장 적은 입구를 자동 선택한다. 고저차 캠퍼스의 핵심 기능이다
- 계산된 경로는 층 단위 구간으로 나뉘어 안내 화면에 전달된다

### 3.3 계단 없는 경로

- 켜면 계단이 하나라도 포함된 길, 수직 이동 중 엘리베이터가 아닌 것, 통행 불가로 확인된 구간을 **경로에서 아예 제외**한다. 선호가 아니라 엄격한 제약이다
- 접근성 정보는 가능 / 불가 / 미확인의 3상태로 관리하며, **미확인은 불가로 취급**한다 — 접근성 오안내는 일반 오류보다 피해가 크기 때문
- 조건을 만족하는 경로가 없으면 억지로 만들지 않고 "확인된 무단차 경로가 없습니다"라고 알린다
- 명칭은 "계단 없는 경로"로 한다. 휠체어뿐 아니라 유모차·캐리어·목발 사용자까지 아우른다

---

## 4. 지도 화면 — 캠퍼스 뷰와 실내 층별 뷰

국민대는 단일 건물이 아니라 여러 건물이 모인 캠퍼스이므로, 지도는 두 단계로 동작한다. 기본 화면은 캠퍼스 전경이고, 실내는 특정 건물을 선택했을 때 열린다.

### 4.1 캠퍼스 뷰 (기본 화면)

- 앱을 열면 보이는 화면. 건물들의 외곽 폴리곤과 실외 장소(광장·운동장·정류장 등)를 표시한다
- 이 단계에서 실내는 보이지 않고, 층 버튼도 없다
- 실외 길찾기 경로선은 이 뷰 위에 그려진다

### 4.2 실내 층별 뷰 (건물 선택 시)

- 진입 방법 두 가지: ① 캠퍼스 뷰에서 건물을 탭 ② 검색 결과 카드의 [지도에서 보기] — 이 경우 캠퍼스 뷰를 거치지 않고 곧장 해당 층이 열리며 대상 장소가 강조된다
- 진입하면 층 선택 버튼(예: 7 / 6 / … / B2)이 나타나고, 선택한 층의 방·코어·엘리베이터·계단이 표시된다 — 네이버지도 코엑스 실내지도와 같은 문법
- 경로 안내 중이 아니어도 층을 넘기며 그 층에 무엇이 있는지 둘러볼 수 있고, 방을 누르면 장소 카드가 열린다
- 지도를 축소하거나 뒤로 가면 캠퍼스 뷰로 복귀한다

### 4.3 실내 지도 표현

- 방은 실제 크기와 모양의 **면(폴리곤)으로 그리고 호실 번호를 표기**한다. 실내에는 파란 점이 없으므로, 사용자가 눈앞의 문 번호와 지도를 대조해 스스로 위치를 확인하는 것이 이 표기의 역할이다
- 복도, 화장실·계단 같은 코어 공간, 엘리베이터·계단 아이콘을 함께 표시한다

### 4.4 경로선

- 경로는 진행 방향 화살표(셰브론)가 반복되는 굵은 선으로 그린다 — 네이버지도의 도보 경로선과 같은 문법
- 실외 구간은 캠퍼스 뷰 위에 GPS 파란 점과 함께, 실내 구간은 해당 건물의 층별 뷰 위에 파란 점 없이 화살표 선만 그린다

### 4.5 층 전환 (경로 안내 중)

- 경로가 층을 넘으면 현재 층 도면 위에 그 층의 화살표 구간만 표시한다
- 층을 넘는 지점에서 경로선은 엘리베이터·계단 아이콘에서 끝나고, 상단의 **층 전환 칩(예: 4F → 15F)** 을 탭하거나 스와이프하면 다음 층 도면과 이어지는 화살표로 넘어간다
- 문구 없이 아이콘·화살표·칩만으로 처리한다

### 4.6 경로 요약

총 소요 시간, 거리, 층 이동 횟수를 요약 칩으로 표시한다. "계단 없는 경로" 토글을 함께 둔다.

### 4.7 입체 표현 (확장 옵션)

지도를 기울이고 건물·층을 블록으로 돌출시키는 2.5D 표현이 가능하다. 필수 기능이 아닌 시연·차별화 카드로 보류한다.

---

## 5. AR 안내 (3순위, 최종 단계)

- 위 지도 안내 위에 얹는 표시층이다. 카메라 화면에 방향 화살표와 다음 지점을 겹쳐 보여준다
- 실내에서 위치·방향을 파악하는 방식은 후보를 검증한 뒤 결정한다: 기존 부착물 인식(호실 명판·안내판) / 전용 마커 부착(학교 협의 필요) / 공간 스캔(구축 비용 큼, 반복되는 복도 구조에 취약)
- **폴백은 기본 지도 화면이다.** AR 미지원 기기나 인식 실패 시에도 4장의 지도+화살표가 그대로 동작하므로 별도 폴백 화면이 필요 없다
- 실외 AR은 GPS와 나침반 기반 화살표(보조)로, 방위각 떨림을 완화해 표시한다

---

## 6. 부서용 기능 (3순위)

- **데이터 관리** — 장소·경로 정보의 등록과 수정. 초기에는 파일 직접 관리, 이후 관리자 화면으로 전환
- **캠퍼스 이용 통계** — 방문, 검색어, 경로 조회, 완주율을 개인 식별이 불가능한 형태로 집계해 제공
- **행사 홍보** — 부서·학생회가 행사를 등록하면 지도 위에 행사 핀·배너를 노출하고 현장까지 길안내를 연결한다. 노출 기간을 설정할 수 있고, 노출·클릭 수를 집계해 도달률을 제공한다

---

## 7. 기술 구성

| 계층 | 선택 |
|---|---|
| 지도 렌더링 | MapLibre GL JS — 데이터를 벡터로 그리며, 색·선 등 겉모습은 전적으로 우리가 정의 |
| UI 프레임 | Next.js(React) · 컴포넌트 레이어로 Astryx 검토 · 모션 anime.js |
| 데모 폴백 | SVG 렌더러 — 미리보기 환경용. 데이터는 동일, 렌더러만 교체 |

---

## 8. 예외 상황 정리

| 상황 | 동작 |
|---|---|
| 검색한 호실이 미등록 | "등록되지 않은 장소" 안내, 건물 카드·유사 검색어 제안 |
| 이어지는 경로가 없음 | 사유와 함께 경로 없음 안내 |
| 목표 입구가 닫힌 시간 | 열려 있는 차선 입구로 재계산 |
| 계단 없는 경로가 없음 | "확인된 무단차 경로 없음" 정직 고지 |
| AR 인식 실패·미지원 | 기본 지도+화살표로 계속 진행 |
| GPS 수신이 나쁜 구역 | 파란 점 의존을 낮추고 경로선 중심으로 표시 |
| 나침반 권한 거부(iOS) | 화살표 회전 없이 경로선만 표시 |
| 층 전환을 놓침 | 층 전환 칩 상시 노출, 수직 이동 아이콘 강조 |
